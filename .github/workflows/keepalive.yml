name: keepalive

on:
  schedule:
    - cron: '*/10 * * * *'   # a cada 10 min (margem contra atrasos do GitHub)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Render
        run: |
          curl -f -s https://exlify.onrender.com
